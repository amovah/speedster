import React from 'react';
import {
  Layout,
  Button,
} from 'antd';

const {
  Header,
} = Layout;


export default () => (
  <Header>
    <Button type="primary">
      Add Url
    </Button>
  </Header>
);
