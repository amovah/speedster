import { createMuiTheme } from '@material-ui/core/styles';

export default createMuiTheme({
  palette: {
    primary: {
      main: '#01579b',
      light: '#4f83cc',
      dark: '#002f6c',
      contrastText: 'white',
    },
    background: {
      default: '#eeeeee',
    },
  },
});
